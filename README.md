# ChessMeet v1.4.2 — Railway Ready

## Что нового в 1.4.0

- рабочая Telegram deep-link атрибуция `ref_<telegram_id>`;
- защита от саморефералов, повторной привязки и двойных наград;
- активация реферала только после создания заявки или отклика;
- 10 очков за активного друга и уровни «Напарник», «Организатор», «Амбассадор»;
- реферальный прогресс, история приглашений и статистика в админ-боте.

## Что нового в 1.3.0

- умная сортировка заявок по городу, уровню и рейтингу;
- лист ожидания с автоматическим повышением при отмене участника;
- процент надёжности игрока на основе сыгранных встреч и no-show;
- мониторинг доступности и ежедневная внешняя копия БД через админ-бота;
- расширенная русская и английская локализация новых сценариев.

Telegram Mini App для поиска офлайн-шахматных партий по городам Беларуси и России.

## Что внутри

- Telegram Bot + Mini App.
- Выбор города, строгая фильтрация заявок и локальный часовой пояс ежедневной задачи.
- Полноценная форма отклика с альтернативной датой, временем и быстрыми шаблонами.
- Расширенные фильтры поиска, городская статистика и подписка на новые партии.
- Check-in «Я на месте», Telegram Back Button, Haptics и продуктовая аналитика.
- Управление откликами автора заявки: просмотр кандидатов, принятие и отклонение прямо в Mini App.
- Безопасный check-in только рядом со временем встречи, включая предупреждение об опоздании.
- Сортировка заявок по расстоянию, популярные шахматные места города и их рейтинг.
- Устанавливаемая PWA-оболочка с network-first кешем для нестабильного соединения.
- Отдельный защищённый Telegram admin-бот: статистика, пользователи, заявки,
  жалобы, блокировки, рассылки, backups, состояние системы и журнал действий.

## Admin bot

Admin-бот запускается рядом с основным ботом, но использует отдельный Telegram
Bot Token и обращается к приложению только через защищённый Admin API.

```env
ADMIN_BOT_TOKEN=...
ADMIN_TELEGRAM_IDS=123456789
ADMIN_OWNER_IDS=123456789
ADMIN_API_URL=https://your-service.up.railway.app
```

`ADMIN_TELEGRAM_IDS` — белый список администраторов. Только ID из
`ADMIN_OWNER_IDS` могут делать массовые рассылки и скачивать базу. Все
блокировки, отмены заявок и решения по жалобам записываются в аудит.
- Заявки на офлайн-партии.
- Профили с фото, рейтингом, значками и настройками темы.
- Отклики, принятие заявки и чат между игроками.
- Напоминания о партии.
- Ежедневная задача «мат в 1» и серия.
- Система жалоб, no-show, блокировок и модерации.
- Админ API для удалённой админки после Railway.
- Подготовка к Railway deploy.

## Города и часовые пояса v0.15.0

Доступны областные центры Беларуси и 10 крупнейших городов России. Заявки показываются только для выбранного города, а новая ежедневная задача и серия переключаются в 00:00 по его местному времени:

- решил сегодня — серия активна;
- решил вчера — серия ещё активна, можно продлить сегодня;
- последний решённый день старше вчера — текущая серия сбрасывается в 0;
- лучший рекорд и общее число решённых задач сохраняются.

Проверка серии вызывается при входе пользователя, bootstrap, задачке дня, профиле, напоминаниях и admin health.

## Локальный запуск

```powershell
py -3.11 -m venv .venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
uvicorn src.main:app --host 0.0.0.0 --port 8000
```

Проверка:

```text
http://localhost:8000/health
```

## Railway

См. подробную инструкцию:

```text
README_DEPLOY_RAILWAY.md
```

Ключевые файлы:

```text
railway.toml
Procfile
run.sh
.env.example
.gitignore
```

## Remote Admin

После Railway локальный launcher больше не должен читать SQLite-файл напрямую. Используй:

```text
chessmeet_remote_admin.py
RUN_REMOTE_ADMIN_CLIENT.bat
```

Он подключается к Railway URL через `ADMIN_TOKEN`.
